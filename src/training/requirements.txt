# Isaac Sim protected packages - MUST match bundled extension versions
# Isaac Sim 5.0/Isaac Lab 2.2.0 ships with scipy 1.15.x in omni.pip.compute
# The bundled scipy requires _transition_to_rng (added in scipy 1.14.0)
# IMPORTANT: isaaclab/isaaclab-tasks require numpy<2
numpy>=1.26.0,<2.0.0
scipy>=1.15.0,<1.16.0

# Azure SDK
azure-core==1.28.0
azure-storage-blob==12.17.0
azure-identity==1.13.0
azure-ai-ml==1.19.0
azureml-mlflow==1.60.0.post1
marshmallow>=3.5,<3.23.0
mlflow>=2.0.0,<3.0.0
packaging==23.0
psutil>=5.9.0  # CPU, memory, disk metrics
pynvml>=11.5.0  # NVIDIA GPU metrics (optional)
pyperclip==1.8.0
rsl-rl-lib==3.0.1
skrl>=1.4.3
tensordict>=0.7.0  # Required by RSL-RL 3.x for TensorDict observations
